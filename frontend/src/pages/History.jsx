import {useEffect,useState} from 'react'; import {Link} from 'react-router-dom'; import {getInspections} from '../services/api';
export default function History(){const [items,setItems]=useState([]);useEffect(()=>{getInspections().then(setItems)},[]);return <section className="page"><header><div><p className="eyebrow">REPOSITORY</p><h1>Inspection history</h1><p>Searchable inspection records will expand in Phase 2.</p></div><Link className="primary" to="/inspect">+ New inspection</Link></header><div className="panel"><table><thead><tr><th>Date</th><th>Product</th><th>Brand</th><th>Status</th><th>Score</th><th></th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{new Date(i.createdAt).toLocaleString()}</td><td>{i.product.productName}</td><td>{i.product.brand}</td><td><span className={`status ${i.analysis.status.toLowerCase()}`}>{i.analysis.status.replace('_',' ')}</span></td><td>{i.analysis.score}%</td><td><Link to={`/inspection/${i.id}`}>Open</Link></td></tr>)}</tbody></table>{!items.length&&<div className="empty">No inspections found.</div>}</div></section>}
