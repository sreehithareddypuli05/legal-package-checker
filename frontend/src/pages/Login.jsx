import {useState} from 'react'; import {ShieldCheck} from 'lucide-react'; import {useNavigate} from 'react-router-dom';
export default function Login(){const [email,setEmail]=useState('inspector@demo.gov.in');const [pass,setPass]=useState('inspector');const nav=useNavigate();return <div className="login"><div className="login-card"><div className="login-logo"><ShieldCheck/></div><h1>MetrologyCheck</h1><p>Legal Metrology Compliance Checker</p><form onSubmit={e=>{e.preventDefault();localStorage.setItem('lm_user',JSON.stringify({email,role:'Inspector'}));nav('/dashboard')}}><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={pass} onChange={e=>setPass(e.target.value)} required/></label><button>Sign in as Inspector</button></form><small>Phase 1 demo authentication</small></div></div>}
