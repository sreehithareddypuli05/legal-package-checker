import{useEffect,useState}from'react';import{Link,useParams}from'react-router-dom';import{CheckCircle2,AlertTriangle,ArrowLeft}from'lucide-react';import{BACKEND,getInspection}from'../services/api';export default function Result(){const{id}=useParams();const[item,setItem]=useState();useEffect(()=>{getInspection(id).then(setItem).catch(console.error)},[id]);if(!item)return <section><p>Loading OCR results…</p></section>;const a=item.analysis||{};return <section><Link className="back" to="/history"><ArrowLeft size={16}/> Inspection history</Link><header><div><p className="eyebrow">OCR INSPECTION REPORT</p><h1>{item.product.productName}</h1><p>{item.product.brand} · {item.inspection.location||'Location not supplied'}</p></div><span className={`big-status ${String(a.status).toLowerCase()}`}>{String(a.status).replace('_',' ')}</span></header><div className="result-summary"><div><strong>{a.score}%</strong><span>Compliance score</span></div><div><strong>{(a.results||[]).length}</strong><span>Declarations detected</span></div><div><strong>{(a.violations||[]).length}</strong><span>Violations</span></div></div><div className="panel"><h2>OCR declaration checklist</h2>{(a.results||[]).map(r=><div className="check-row" key={r.field}><div className={`check-icon ${r.valid?'ok':'bad'}`}>{r.valid?<CheckCircle2/>:<AlertTriangle/>}</div><div className="check-main"><strong>{r.label}</strong><span>{r.extractedText||'Not detected by OCR'}</span></div><div><span className={`status ${r.valid?'compliant':'non_compliant'}`}>{r.valid?'PASS':'FLAG'}</span><small>{r.ruleRef}</small></div></div>)}</div><div className="panel"><h2>Uploaded evidence</h2><div className="image-grid">{(item.images||[]).map(x=><img key={x.name} src={`${BACKEND}${x.path}`} alt={x.name}/>)}</div></div></section>}
