import type {DeclarationData,Check} from '../types';
const required:[string,keyof DeclarationData,string][]=[
 ['Manufacturer / Packer','manufacturer','Rule 6'],['Common / Generic Name','commonName','Rule 6'],['Net Quantity','netQuantity','Rule 6'],['MRP — Maximum Retail Price','mrp','Rule 6'],['Month & Year of Packing / Manufacture','packedDate','Rule 6'],['Best Before / Use By','bestBefore','Food label check'],['Batch / Lot Number','batchNumber','Food label check'],['Consumer Care Details','consumerCare','Rule 6'],['Country of Origin','countryOfOrigin','Rule 6'],['Unit Sale Price','unitSalePrice','Rule 6'],['Readability','readability','Declaration readability']
];
export function runChecks(d:DeclarationData):Check[]{return required.map(([field,key,rule])=>{const value=String(d[key]||'').trim();if(!value)return {field,value:'[MISSING]',status:'FAIL',reason:`Required declaration is missing from the prototype extraction profile.`,rule};if(key==='mrp'&&!/^₹\s*\d+(?:\.\d{1,2})?$/.test(value))return {field,value,status:'FAIL',reason:'MRP must be presented as a monetary maximum retail price, e.g. ₹30.00.',rule};if(key==='netQuantity'&&!/^\d+(?:\.\d+)?\s*(g|kg|ml|L|l)$/.test(value))return {field,value,status:'FAIL',reason:'Net quantity must use a standard unit such as g, kg, ml or L.',rule};if(key==='readability'&&/poor/i.test(value))return {field,value,status:'FAIL',reason:'Declaration text is marked as difficult to read in the prototype evidence profile.',rule};return {field,value,status:'PASS',reason:'Declaration is present and passes the prototype validation rule.',rule};});}
export function getOverall(checks:Check[]):'COMPLIANT'|'NON-COMPLIANT'{return checks.every(c=>c.status==='PASS')?'COMPLIANT':'NON-COMPLIANT'}
